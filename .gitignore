node_modules/
.env
.env.local
npm-debug.log*
*.log

# never commit a memory database or credentials
*.db
*.db-journal
*.sqlite
.claude/settings.local.json

# OS noise
Thumbs.db
.DS_Store

# the XTTS python environment (~5 GB) and its audition WAVs — see README
.venv-xtts/
voicelab/samples/

# the app document: tasks, journal entries, the routine — personal, and local
data/
