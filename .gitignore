node_modules/
.env
.env.local
npm-debug.log*
*.log

# never commit a memory database or credentials
*.db
*.db-journal
*.sqlite
.claude/settings.local.json

# OS noise
Thumbs.db
.DS_Store

# the XTTS python environment (~5 GB) and its audition WAVs — see README
.venv-xtts/
voicelab/samples/

# Kacey's own records now live in klaus_memory's SQLite file (see db.js).
# data/ remains ignored: it holds the pre-SQLite app-state.json on machines
# that have not started the new build yet, plus its .migrated copy afterwards.
data/
